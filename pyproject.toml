[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "P6"
version = "0.0.1"
requires-python = ">=3.10"
readme = "README.md"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
exclude = ["tests"]
